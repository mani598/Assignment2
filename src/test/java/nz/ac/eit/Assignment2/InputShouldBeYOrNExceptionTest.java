package nz.ac.eit.Assignment2;

import org.junit.Test;

import static org.junit.Assert.*;

public class InputShouldBeYOrNExceptionTest {

    @Test
    public void getMessage() {
//assertEquals("The input must be either 'y' or 'n'.");
        //still not quite got my head around this, will try to get it working later. Commented out for now
        //so it won't cause errors

    }
}